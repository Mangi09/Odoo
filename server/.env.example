PORT=5000

# Use these values if you created the database in pgAdmin.
PGHOST=localhost
PGPORT=5432
PGDATABASE=traveloop
PGUSER=postgres
PGPASSWORD=your_postgres_password

# Optional: restrict this to the frontend URL, for example http://localhost:5173
CORS_ORIGIN=*
